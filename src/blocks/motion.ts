import type { BlockImpl } from "./types.ts";

export const motion_movesteps: BlockImpl = {
  topLevel: false,
  generate() {
    return ''
  }
}
export const motion_turnright: BlockImpl = {
  topLevel: false,
  generate() {
    return ''
  }
}
export const motion_ifonedgebounce: BlockImpl = {
    topLevel: false,
    generate() {
        return ''
    }
}